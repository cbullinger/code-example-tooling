---
apiVersion: v1
kind: ConfigMap
metadata:
  name: github-metrics-config
  namespace: docs
data:
  # Add any non-sensitive configuration here
  # For example:
  # LOG_LEVEL: "info"
  # METRICS_INTERVAL: "3600"
  # DATABASE_NAME: "github_metrics"
  # COLLECTION_NAME: "metrics"

