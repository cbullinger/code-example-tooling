---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tool-jobs-sa
  namespace: docs
  annotations:
    # Add any necessary annotations for AWS IAM roles or other integrations
    # For example, if using IRSA (IAM Roles for Service Accounts):
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/ROLE_NAME

