# Test Dockerfile for running HTTP server instead of cron job
# This allows you to verify secrets are properly injected

FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY github-metrics/package*.json ./
RUN npm ci --omit=dev

# Copy the test server
COPY github-metrics/test-server.js ./

# Expose port 8080
EXPOSE 8080

# Run the test server
CMD ["node", "test-server.js"]

